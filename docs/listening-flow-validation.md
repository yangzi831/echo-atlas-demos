# 聆听交互修复与验证

核实日期：2026-09-13。

参考 `cd4a0b9`、`7e963e9`、`d421fa9` 的连续转写、语义分析与服务端转发实现；在反向提交后的当前视觉版本上按需恢复依赖，没有恢复整个合并提交。

- 语音输入和开始按钮改为顺序布局。桌面输入区域独立滚动，开始按钮保留在下方；窄屏页面顺序滚动。
- 输入阶段显示识别中状态，识别时禁止编辑和启动硬件会话，避免两个收音阶段同时进行。
- 持续聆听展示实时原文、AI 上下文摘要、候选记忆与原文证据，支持确认、撤回和补充理解。
- 开发环境调用同源转发接口，端口变化不再受云端来源白名单影响；未保存个人配置时使用本地服务端环境配置。线上继续使用既有专用转发服务。
- 本地可选工作空间配置导致 Workspace.AccessDenied；清空后，原密钥在默认工作空间成功启动 Fun-ASR。密钥没有变更。

## 已验证

- 生产构建通过；仍存在原有大体积 bundle 提示。
- 8 项自动化测试通过：时间定位、证据校验、串行批次、结束收尾、失败重试和配置隔离。
- 浏览器验证 1280×720 与 390×844 下长文字不会与开始按钮重叠。
- 使用系统合成的约 8 秒测试语音，经本地 WebSocket → Fun-ASR 获得带时间戳转写，再经 ApiMux 的 gpt-5.6-sol 获得 1 条匹配记忆和上下文摘要。该音频为合成测试素材。

## 尚未验收

实际硬件长时间持续录音、断连恢复，以及用户真实麦克风语音输入。本次服务测试不能代替真机验收。

## 语音入口视觉调整

取消首页外层卡片、文字编辑框与手动确认步骤。轻触麦克风后进行语音识别，浏览器结束识别时自动沿用转写作为约定；转写以只读文字浮现，重新说话可更新约定。保留独立的开始按钮以触发硬件选择。其他页面的文字输入保持原有能力。已完成构建及浏览器无输入框检查。

## 持续聆听悬浮布局

移除大标题、日期说明和处理流水线文案。动画铺满画面，最新一句转写悬浮显示（完整转写仍在后台保存），AI 回响和候选记忆采用无卡片布局，约定缩为角落小字。片段详情按需展开，保留确认和撤回。生产构建通过，并在浏览器检查连接态布局。

## 字幕、片段播放与密钥检查

字幕同句更新直至结束标识到达，结束后保留到下一句；如果旧草稿缺少结束标识，新句仍会替换旧草稿，避免卡住。已增加状态选择测试。候选记忆直接显示原音频起止时间与播放/暂停入口。字幕改为偏左、小字号；右上角仅在连接状态显示设备名称及设备元数据中的采样率和传输格式。

2026-09-13 检查本地两项实际密钥：当前非忽略文件、dist 产物、895 个可达 Git 对象中未发现匹配；文本文件的常见 sk- token 模式检查未命中。/.env.local 返回 403。检查范围不包含远端已删除历史或第三方日志。现有个人设置仍会将用户填写的密钥放在浏览器 localStorage 并传给转发接口；本地服务端环境密钥不会下发浏览器。

修复字幕选择回归：原逻辑选择最早未完成句，可能持续停在“好”，虽然后台已经处理后续转写。改为按音频开始时间显示最新非空句；测试覆盖旧草稿缺失结束标识、乱序输入、新句完成和空句，不更改原始转写证据。

保留上一条已完成字幕为小号淡文字。GPT 状态来自语义处理实际开始/完成回调。播放器改为独立管理 Blob URL，避免 React 选中状态重复赋值 src 中断播放；支持同片段续播、定位和快速换片段。11 项测试和构建通过，浏览器静音播放合成 8.506 秒 WAV 完整结束，未出现媒体错误。未据此声称用户硬件录音已全部验收。
